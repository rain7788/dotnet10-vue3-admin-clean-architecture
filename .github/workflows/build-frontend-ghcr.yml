name: build-frontend-images

on:
  push:
    branches: [main]
    paths:
      - 'web-admin/**'
      - '.github/workflows/build-frontend-ghcr.yml'
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Tencent TCR
        uses: docker/login-action@v3
        with:
          registry: ccr.ccs.tencentyun.com
          username: ${{ secrets.TCR_USERNAME }}
          password: ${{ secrets.TCR_PASSWORD }}

      - name: Build and push (frontend)
        uses: docker/build-push-action@v6
        with:
          context: ./web-admin
          file: ./web-admin/Dockerfile
          push: true
          tags: |
            ghcr.io/rain7788/art-web-admin:latest
            ghcr.io/rain7788/art-web-admin:sha-${{ github.sha }}
            ccr.ccs.tencentyun.com/rain7788/art-web-admin:latest
            ccr.ccs.tencentyun.com/rain7788/art-web-admin:sha-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to k3s
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: root
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          command_timeout: 65m
          script: |
            flock --wait 1800 /var/lock/art-admin-deploy.lock sh -eu -c '
              target_image=ccr.ccs.tencentyun.com/rain7788/art-web-admin:latest
              k3s kubectl -n apps patch deploy art-web-admin --type merge -p "{\"spec\":{\"progressDeadlineSeconds\":1800}}"
              current_image="$(k3s kubectl -n apps get deploy art-web-admin -o jsonpath="{.spec.template.spec.containers[0].image}")"
              if [ "$current_image" = "$target_image" ]; then
                k3s kubectl -n apps rollout restart deploy art-web-admin
              else
                k3s kubectl -n apps set image deploy/art-web-admin art-web-admin="$target_image"
              fi
              k3s kubectl -n apps rollout status deploy art-web-admin --timeout=30m
            '
